{% extends 'base.html.twig' %}

{% block title %}Year{% endblock %}

{% block body %}
    <h1>Year</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ year.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_app_year_index') }}">back to list</a>

    <a href="{{ path('back_app_year_edit', {'id': year.id}) }}">edit</a>

    {{ include('year/_delete_form.html.twig') }}
{% endblock %}
